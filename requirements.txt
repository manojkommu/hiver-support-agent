# Core data / baselines (run anywhere, no API needed)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Local embeddings for retrieval (downloads a small model on first run)
sentence-transformers>=2.2

# Gemini (free tier). Google's current unified SDK.
google-genai>=0.3
python-dotenv>=1.0
